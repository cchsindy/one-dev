const state = {
  user: 'bradspencer'
}

const getters = {
  user(state) {
    return state.user
  }
}

const actions = {

}

const mutations = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
